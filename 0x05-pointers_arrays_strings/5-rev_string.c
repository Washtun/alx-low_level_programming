#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * main - funtion to reverse a string
 *
 * Return: Always 
 */

void rev_string(char *s)
{
	int len, j;
	len = strlen(s);

	for(j = len - 1; j >=0; j--)
		return(s[j]);


}
