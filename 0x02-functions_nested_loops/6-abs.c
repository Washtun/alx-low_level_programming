#include "main.h"
#include "holberton.h"
#include <stdio.h>


/* Function to find the absolute value*/

int _abs(int N)
{
    

    /* If the number is less than
     * zero, then multiply by (-1)
     */
    
	 if (N < 0) 
	 {
        	N = (-1) * N;
    	 }
	
         else
		 return (N);

}
